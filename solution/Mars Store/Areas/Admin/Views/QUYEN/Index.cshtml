@model IEnumerable<Mars_Store.Models.Entities.QUYEN>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_layoutAdmin.cshtml";
}

<p>
    @Html.ActionLink("THÊM QUYỀN", "Create")
</p>
<table class="table">
    <tr>
        <th>
            MÃ QUYỀN
        </th>
        <th>
            TÊN QUYỀN
        </th>
        <th>
            TÁC VỤ
        </th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ID_QUYEN)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TENQUYEN)
    </td>
    <td>
        @Html.ActionLink("SỬA", "Edit", new {id=item.ID_QUYEN}) |
        @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
        @Html.ActionLink("XÓA", "Delete", new {id=item.ID_QUYEN})
    </td>
</tr>
}

</table>
